using UnityEngine;

public class interaction : MonoBehaviour
{
    // Här försöker jag göra UI för knappet E när spelaren är nära glass/ så att spelaren vet vad ska hen göra.


    //bool IsPlayerNear = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /*if (Input.GetKeyDown(KeyCode.E) && IsPlayerNear == true)
        {
            print("break the glass wall!!");
        }
    }
    private void OnTriggerEnter2D(Collider2D collision)
    {
        print("Glas väg är nära mig");
        IsPlayerNear = true;
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        print("glaset är inte nära mig");
        IsPlayerNear = false;*/
    }
}
